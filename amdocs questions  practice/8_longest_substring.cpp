#include<bits/stdc++.h>
using namespace std;

string sub(string s){
    unordered_map<char, int> freq;
    int maxlength = 0, start = 0;

        for(int end = 0; end <s.length(); end++){
            if(freq.find(s[end]) != freq.end() && freq[s[end]] >= start){

            }
        }
}

int main(){

}